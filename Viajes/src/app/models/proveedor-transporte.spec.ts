import { ProveedorTransporte } from './proveedor-transporte';

describe('ProveedorTransporte', () => {
  it('should create an instance', () => {
    expect(new ProveedorTransporte()).toBeTruthy();
  });
});
